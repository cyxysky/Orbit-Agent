import type { CapabilitySkill } from '@webpilot/capability-sdk';
export const communicationRuntimeSkillId = 'system-communication-runtime';
export const communicationRuntimeSkill = Object.freeze({
  id: communicationRuntimeSkillId, title: 'Communication Runtime',
  summary: `<system_skill id="${communicationRuntimeSkillId}">Create an exact draft before sending any external message; inspect each channel's target and content capabilities, verify the draft, and rely on host approval for send.</system_skill>`,
  content: `# Communication Runtime\n\n- For communication requests, discover configured channels before asking the user to identify a system or repeat a destination. Refresh channels after configuration changes; current tool results take precedence over earlier conversation claims.\n- Always list channels first and respect the selected channel's targetKinds, contentFormats, and requiresExplicitTargets capabilities.\n- Use image, file, voice, or video content for actual media delivery. Supply exactly one artifactId returned by file/media tools or real mediaId returned by the same provider. Check mediaSources: artifactId requires channel media upload support. For WeCom this uses the official bot WebSocket connection with Bot ID and Secret, not a separate HTTP upload URL. Never invent mediaId, put a URL in mediaId, or replace media with Markdown images or placeholder text.\n- Use separate drafts for text captions and media. Video supports title and description; other media formats do not. WeCom voice requires AMR; send other audio formats as files when conversion is unavailable.\n- If mediaSources does not include artifactId, report the missing bot credentials or channel media configuration instead of claiming media was sent. The host uploads artifact bytes only inside an approved send.\n- Treat provider IDs as opaque strings. Never infer that media_id is fabricated from its length, hexadecimal or base64 appearance. Report the exact failing stage and provider error; an uploaded ID does not prove send acceptance. Never mix WebSocket media/recipient IDs with MCP IDs. Select destinations from availableTargets or a configured verified default. Copy any transport field along with kind and id. For WeCom artifactId uploads, explicitly select a wecom-websocket target from availableTargets; settings defaults and wecom-mcp targets use MCP and cannot accept WebSocket uploads.\n- Always create and inspect a draft before send. A failed delivery means no recipient accepted it; after correcting the cause, retry the same draft. A sent draft returns its original receipt without resending. A sending or unknown delivery needs receipt verification; do not loop send or claim that a retry reached the provider when it was blocked locally.\n- Separate upload acceptance, send acceptance, and recipient read confirmation. Use receipt acceptedAt as the send time; never use the last incoming message time.\n- Preserve exact target identifiers and target kinds; never guess a user ID, group ID, email address, or audience.\n- Sending is externally visible and requires host approval even when a draft exists.\n- Do not include credentials or unrelated private information.\n- Report delivery identifiers only after the channel confirms acceptance.`,
  required: true, activation: [{ toolName: 'communication', actions: ['channels', 'draft', 'readDraft', 'send'] }],
} satisfies CapabilitySkill);
