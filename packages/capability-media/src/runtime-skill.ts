import type { CapabilitySkill } from '@webpilot/capability-sdk';
export const mediaRuntimeSkillId = 'system-media-runtime';
export const mediaRuntimeSkill = Object.freeze({
  id: mediaRuntimeSkillId, title: 'Media Runtime',
  summary: `<system_skill id="${mediaRuntimeSkillId}">Inspect media before expensive processing, use bounded frame/OCR/transcription scopes, and label generated versus source media accurately.</system_skill>`,
  content: `# Media Runtime\n\n- Inspect the source before choosing OCR, transcription, or frame extraction.\n- Use the smallest adequate page, time, language, and frame scope.\n- Preserve timestamps and source references with extracted evidence.\n- OCR and transcription are probabilistic; flag uncertain names, numbers, and inaudible text.\n- Use listModels to discover configured image, video, and speech models. modelRef is the returned configuration id; omit it to use the selected model of that type. Host-selected image, video and speech models are independent of the language model and take precedence over modelRef. Never invent model ids or API endpoints.\n- generateImage accepts prompt, optional sourceRefs and maskRef for image editing, size and count. generateVideo accepts prompt, at most one starting image in sourceRefs, duration, size and aspectRatio. generateSpeech converts the exact prompt text into speech, with optional voice, language and outputFormat. It does not generate music or sound effects.\n- Reference inputs must be registered attachment ids or artifact URLs. Model credentials and service paths belong to host settings, never tool arguments.\n- Video generation waits for the provider job to complete. Do not resubmit a timed-out generation automatically, since the remote job may still run.\n- Clearly label generated images, videos and audio; never present them as source evidence. Artifact-producing actions return each saved file once in data. Use the exact returned url for Markdown image embeds and inline media, and downloadUrl for download links. Copy these URLs verbatim: a leading slash is an application-relative path, not a hostname. Do not add a scheme or invent a domain. Never invent attachment://, sandbox:, file:, or local filesystem URLs.`,
  required: true, activation: [{ toolName: 'media', actions: ['listModels', 'inspect', 'extractFrames', 'ocr', 'transcribe', 'generateImage', 'generateVideo', 'generateSpeech'] }],
} satisfies CapabilitySkill);
